{
  "routes": [
    {
      "method": "GET",
      "path": "/to-watch",
      "handler": "to-watch.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/to-watch/count",
      "handler": "to-watch.count",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/to-watch/:id",
      "handler": "to-watch.findOne",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/to-watch",
      "handler": "to-watch.create",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/to-watch/:id",
      "handler": "to-watch.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/to-watch/:id",
      "handler": "to-watch.delete",
      "config": {
        "policies": []
      }
    }
  ]
}
